CREATE TABLE STUDENT
(USERID VARCHAR(50) PRIMARY KEY,
PASSWORD VARCHAR(20) NOT NULL,
UNAME VARCHAR(15) NOT NULL,
ADDRESS VARCHAR(60),
MOBILENO VARCHAR(20));

CREATE TABLE PROFESSOR
(USERID VARCHAR(50) PRIMARY KEY,
PASSWORD VARCHAR(20) NOT NULL,
UNAME VARCHAR(15) NOT NULL,
ADDRESS VARCHAR(60),
MOBILENO VARCHAR(20));

CREATE TABLE ADMIN
(USERID VARCHAR(50) PRIMARY KEY,
PASSWORD VARCHAR(20) NOT NULL,
UNAME VARCHAR(15) NOT NULL,
ADDRESS VARCHAR(60),
MOBILENO VARCHAR(20));

CREATE TABLE SentResume(emailid,name,qual,spec,exp,conctact,date);

create table PermittedProfessors(email-id ,alotted password,alloted id)
create table declinedRequests(userid varchar(50),message varchar(50),);

